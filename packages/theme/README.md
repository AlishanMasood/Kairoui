# @kairoui/theme

Theme engine providing runtime theming, token resolution, and theme context for KairoUI components.

## Responsibilities

- Theme provider and context
- Runtime theme switching (light/dark/custom)
- Token-to-CSS-variable resolution
- Theme composition and extension API
- SSR-safe theme hydration

## Status

Package skeleton — implementation pending.
