import type ts from "typescript";
import type { PropMeta } from "./schema";

/**
 * Extracts prop metadata from a TypeScript interface symbol.
 * Stub — full implementation in KUI-DOCGEN-003.
 */
export function extractPropsFromSymbol(_checker: ts.TypeChecker, _symbol: ts.Symbol): PropMeta[] {
  return [];
}
