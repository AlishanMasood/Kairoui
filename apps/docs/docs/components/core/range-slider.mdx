---
title: RangeSlider
sidebar_position: 15
---

import { ComponentHeader, ImportStatement, DocsSection, Callout, PropsTable } from "@kairoui/docs";
import { getComponentProps } from "@site/src/api-data";

<ComponentHeader
  name="RangeSlider"
  description="Dual-thumb range selection with collision prevention, minimum distance enforcement, and per-thumb accessibility."
  package="@kairoui/core/components"
  status="beta"
/>

<ImportStatement
  imports={["RangeSlider", "SliderTrack", "SliderRange", "SliderThumb"]}
  from="@kairoui/core/components"
/>

<DocsSection title="API">

<PropsTable props={getComponentProps("RangeSlider")} />

</DocsSection>

<Callout type="info" title="Thumb collision">
  Thumbs cannot cross each other. Use `minStepsBetweenThumbs` to enforce a minimum gap.
</Callout>

<Callout type="info" title="Reuses Slider parts">
  RangeSlider uses the same `SliderTrack`, `SliderRange`, and `SliderThumb` components. Render two
  `SliderThumb` elements with `index={0}` and `index={1}`.
</Callout>
