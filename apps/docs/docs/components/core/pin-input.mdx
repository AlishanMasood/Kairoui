---
title: PinInput
sidebar_position: 16
---

import { ComponentHeader, ImportStatement, DocsSection, Callout, PropsTable } from "@kairoui/docs";
import { toPropsList } from "@site/src/api-data";
import PinInputMeta from "@site/src/generated/components/PinInput.json";

<ComponentHeader
  name="PinInput"
  description="Segmented code entry for OTP, verification codes, and PIN input with auto-advance, paste support, and mobile-friendly keyboard."
  package="@kairoui/core/components"
  status="beta"
/>

<ImportStatement imports={["PinInput"]} from="@kairoui/core/components" />

<DocsSection title="Keyboard">

- **Typing a digit/letter** — fills current field and auto-advances
- **Backspace** — clears current or moves to previous
- **ArrowLeft/Right** — navigate between fields
- **Delete** — clears current field
- **Paste** — distributes text across fields from cursor

</DocsSection>

<DocsSection title="API">

<PropsTable props={toPropsList(PinInputMeta)} />

</DocsSection>

<Callout type="info" title="OTP autofill">
  The first field has `autoComplete="one-time-code"` for browser/mobile OTP autofill.
</Callout>

<Callout type="info" title="Accessibility">
  Each field has `aria-label="Digit N of M"`. The container has `role="group"`. Use with Field and
  Label for full accessible labeling.
</Callout>
