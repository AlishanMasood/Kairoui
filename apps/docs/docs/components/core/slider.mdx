---
title: Slider
sidebar_position: 14
---

import { ComponentHeader, ImportStatement, DocsSection, Callout, PropsTable } from "@kairoui/docs";
import { getComponentProps } from "@site/src/api-data";

<ComponentHeader
  name="Slider"
  description="Single-thumb range input with keyboard navigation, step snapping, and accessible value text."
  package="@kairoui/core/components"
  status="beta"
/>

<ImportStatement
  imports={["Slider", "SliderTrack", "SliderRange", "SliderThumb"]}
  from="@kairoui/core/components"
/>

<DocsSection title="Keyboard">

- **ArrowRight/Up** — increment by step
- **ArrowLeft/Down** — decrement by step
- **Home** — jump to min
- **End** — jump to max
- **PageUp/PageDown** — increment/decrement by 10× step

</DocsSection>

<DocsSection title="API: Slider">

<PropsTable props={getComponentProps("Slider")} />

</DocsSection>

<Callout type="info" title="Accessibility">
  The thumb has `role="slider"` with `aria-valuemin`, `aria-valuemax`, `aria-valuenow`. Use
  `getValueLabel` to provide meaningful text like "50%" for screen readers.
</Callout>
