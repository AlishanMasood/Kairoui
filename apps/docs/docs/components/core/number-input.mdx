---
title: NumberInput
sidebar_position: 13
---

import { ComponentHeader, ImportStatement, DocsSection, Callout, PropsTable } from "@kairoui/docs";
import { toPropsList } from "@site/src/api-data";
import NumberInputMeta from "@site/src/generated/components/NumberInput.json";

<ComponentHeader
  name="NumberInput"
  description="Numeric input with increment/decrement buttons, keyboard controls, min/max clamping, and step precision."
  package="@kairoui/core/components"
  status="beta"
/>

<ImportStatement imports={["NumberInput"]} from="@kairoui/core/components" />

<DocsSection title="Keyboard">

- **ArrowUp** — increment by step
- **ArrowDown** — decrement by step
- Direct numeric typing supported

</DocsSection>

<DocsSection title="API">

<PropsTable props={toPropsList(NumberInputMeta)} />

</DocsSection>

<Callout type="info" title="Precision">
  Step values like `0.1` or `0.01` automatically maintain the correct number of decimal places.
  Values are clamped to min/max on increment, decrement, and blur.
</Callout>

<Callout type="info" title="Accessibility">
  Uses `role="spinbutton"` with `aria-valuemin`, `aria-valuemax`, `aria-valuenow`. Increment and
  decrement buttons have `aria-label` for screen readers.
</Callout>
