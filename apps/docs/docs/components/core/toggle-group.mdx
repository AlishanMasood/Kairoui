---
title: ToggleGroup
sidebar_position: 18
---

import { ComponentHeader, ImportStatement, DocsSection, Callout, PropsTable } from "@kairoui/docs";
import { toPropsList } from "@site/src/api-data";
import ToggleGroupMeta from "@site/src/generated/components/ToggleGroup.json";
import ToggleGroupItemMeta from "@site/src/generated/components/ToggleGroupItem.json";

<ComponentHeader
  name="ToggleGroup"
  description="Group of toggle buttons supporting single or multiple selection with shared styling and keyboard navigation."
  package="@kairoui/core/components"
  status="beta"
/>

<ImportStatement imports={["ToggleGroup", "ToggleGroupItem"]} from="@kairoui/core/components" />

<DocsSection title="API: ToggleGroup">

<PropsTable props={toPropsList(ToggleGroupMeta)} />

</DocsSection>

<DocsSection title="API: ToggleGroupItem">

<PropsTable props={toPropsList(ToggleGroupItemMeta)} />

</DocsSection>

<Callout type="info" title="Single vs Multiple">
  In `type="single"`, clicking a new item deselects the previous. Clicking the same item deselects
  it. In `type="multiple"`, each item toggles independently — multiple can be pressed
  simultaneously.
</Callout>

<Callout type="info" title="Accessibility">
  Uses `role="group"` with `aria-orientation`. Each item has `aria-pressed`. Enter/Space toggle
  items via native button behavior.
</Callout>
